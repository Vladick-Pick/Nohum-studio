# Frontend Developer Tools

Primary operating surfaces:

- Repo/worktree
- tests
- browser if needed
- review package

Required evidence surfaces:

- test output
- screens or notes
- review package

Secrets usually wired after import:

- `OPENAI_API_KEY`
- `GITHUB_TOKEN`

Restrictions:

- No skipping verification
- No self-approval of review or release

Post-import note:

- New specialist. Activate once repo access and test tooling are wired.
- This is a newly introduced specialist or manager. Import as new and keep paused until runtime wiring is complete.
