schema: paperclip/v1
description: Bootstrap runtime defaults for NoHum Studio v1.5 detailed-core org package.

company:
  budgetMonthlyCents: 650000

agents:
  ceo:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 25000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Verify control-plane approvals and governance docs are wired.
      - Exact-parity core slug. Upgrade in place and validate reporting lines after import.
  chief-of-staff:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 18000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Keep wake-on-demand available and wire cadence dashboards after import.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  agent-mechanic:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Grant diagnostic-only access to runtime paths and tool health scripts.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  research-lead:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 18000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        APIFY_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Existing live core slug should upgrade in place.
      - Exact-parity core slug. Upgrade in place and validate reporting lines after import.
  research-synthesizer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 12000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Pause after import until research templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  competitor-scout:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 12000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        APIFY_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Pause after import until browser access and evidence templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  demand-validator:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 12000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        APIFY_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Pause after import until demand evidence template is wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  revenue-validator:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 12000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Pause after import until pricing and payment evidence surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  launch-lead:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 22000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        PAYMENT_PROVIDER_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Existing live core slug should upgrade in place and be rebound as head of Product Launch.
      - Exact-parity core slug. Upgrade in place and validate reporting lines after import.
  product-definer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until Gate B artifacts and templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  ux-researcher:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate only after feedback and research surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  ux-architect:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until design and engineering handoff surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  ui-designer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until design handoff paths are available.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  pricing-strategist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 16000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        PAYMENT_PROVIDER_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until pricing and payment evidence surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  launch-program-manager:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate only after team-level checklists are imported.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  cmo:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 22000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New top-level manager. Keep paused until analytics and campaign destinations are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  growth-lead:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Existing package role moves under CMO; keep paused until analytics destinations are confirmed.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  marketing-strategist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once positioning and message surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  seo-specialist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 14000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once content and analytics surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  content-creator:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 14500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once content destinations and templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  paid-media-strategist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until analytics destinations and campaign surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  tracking-measurement-specialist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Keep paused until analytics destinations and dashboards are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  community-builder:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 14000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once community surfaces and summaries are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  ai-citation-strategist:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once content inventory and measurement surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  vp-engineering:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 23000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New top-level manager. Keep paused until repo and release tooling are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  software-architect:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 19000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once engineering worktree access is wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  backend-architect:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 18000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once backend repo access and CI are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  frontend-developer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once repo access and test tooling are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  ai-engineer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 18000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once repo access and evaluation tooling are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  senior-developer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 18000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once repo and test access are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  devops-automator:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once deploy tooling and secrets are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  sre:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once observability surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  security-engineer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once repo and runtime review surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  code-reviewer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Existing package role moves under VP Engineering; keep paused until repo access is wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  qa-director:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 16000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once QA tooling and test environments are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  qa-engineer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 14500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once QA tooling and test environments are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  release-engineer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 15500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        GITHUB_TOKEN:
          kind: secret
          requirement: optional
    notes:
      - Existing package role moves under VP Engineering; keep paused until release tooling is wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  support-lead:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 17000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        PAYMENT_PROVIDER_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Existing package role becomes a top-level manager; keep paused until support tooling and escalation templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  support-responder:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 13000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        PAYMENT_PROVIDER_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once support queue tooling is wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  feedback-synthesizer:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 13500
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - Existing package role moves under Support Lead; keep paused until support and analytics surfaces are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
  analytics-reporter:
    adapter:
      type: codex_local
      config:
        model: gpt-5.4
        modelReasoningEffort: high
    budgetMonthlyCents: 14000
    inputs:
      env:
        OPENAI_API_KEY:
          kind: secret
          requirement: optional
        ANALYTICS_API_KEY:
          kind: secret
          requirement: optional
    notes:
      - New specialist. Activate once analytics surfaces and report templates are wired.
      - Non-core slug. Preview first: if the slug already exists live, reconcile or update it in place; if it is absent, import it as new and keep it paused until runtime wiring is complete.
