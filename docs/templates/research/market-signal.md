# Research Market Signal

- `market_signal_id`: `ms-YYYYMMDD-slug`
- `captured_at`:
- `captured_by`:
- `source`: `trustmrr` / `product_hunt_manual` / `marketplace` / `github` /
  `community` / `competitor_site` / `search` / `other`
- `source_url`:
- `source_allowed`: `yes` / `no` / `unclear`
- `source_confidence_tier`: `A` / `B` / `C` / `D`
- `raw_snapshot_ref`:

## Observed Object

- `name`:
- `website`:
- `normalized_domain`:
- `description`:
- `category`:
- `audience_hint`:
- `product_shape_hint`:
- `pricing_or_revenue_hint`:

## Signal Strength

- `revenue_proof`: `none` / `weak` / `medium` / `high`
- `demand_proof`: `none` / `weak` / `medium` / `high`
- `pain_proof`: `none` / `weak` / `medium` / `high`
- `channel_proof`: `none` / `weak` / `medium` / `high`
- `buildability_hint`: `none` / `weak` / `medium` / `high`

## Duplicate Check

- `duplicate_key`:
- `duplicate_checked_against`:
- `duplicate_status`: `new` / `duplicate` / `possible_duplicate`
- `duplicate_refs`:

## Research Routing

- `recommended_action`: `IGNORE` / `WATCH` / `OPEN_IDEA_CARD` / `RESEARCH_REQUIRED`
- `reason`:
- `open_questions`:
