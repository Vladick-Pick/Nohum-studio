# Evidence Event

- `evidence_event_id`: `ee-YYYYMMDD-slug`
- `product_bet_id`:
- `assumption_id`:
- `rat_plan_id`:
- `created_at`:
- `created_by`:

## Source

- `source`: `internal_judge` / `research` / `live_market` / `analytics` / `cold_outreach` / `pricing_intent` / `checkout_intent` / `customer_reply` / `payment` / `retention` / `other`
- `source_url_or_ref`:
- `source_snapshot_ref`:
- `source_allowed`: `yes` / `no` / `unclear`

## Metric

- `metric`:
- `observed_value`:
- `threshold`:
- `result`: `pass` / `warn` / `fail` / `inconclusive`
- `confidence`: `low` / `medium` / `high`
- `signal_strength`: `weak` / `medium` / `strong` / `decisive`
- `payment_proximity`: `weak` / `medium` / `strong` / `decisive`
- `cost_cents`:
- `duration_hours`:

## Interpretation

- `what_changed`:
- `what_did_not_change`:
- `decision_impact`:
- `limitations`:
- `follow_up_needed`:

## Audit

- `raw_data_ref`:
- `reviewer`:
- `review_status`: `pending` / `accepted` / `needs_correction` / `rejected`
