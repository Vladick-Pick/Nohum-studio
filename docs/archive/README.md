# Archive

This directory contains historical planning notes, external reference dumps, and
legacy dry-run outputs.

Archived files are not part of the active Paperclip import package and should
not be treated as current runtime doctrine. If an archived idea becomes useful,
promote it by copying the relevant decision into an active `docs/`, `tasks/`, or
`skills/` surface with current ownership and validation.
